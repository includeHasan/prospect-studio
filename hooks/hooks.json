{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/install-deps.sh\""
          },
          {
            "type": "command",
            "command": "bash -c 'PY=$(command -v python3 || command -v python || command -v py); [ -n \"$PY\" ] && \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/scripts/deadline-monitor.py\" || exit 0'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'PY=$(command -v python3 || command -v python || command -v py); [ -n \"$PY\" ] && \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/scripts/track-document.py\" || exit 0'"
          }
        ]
      },
      {
        "matcher": "mcp__serpapi|WebFetch|WebSearch",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'PY=$(command -v python3 || command -v python || command -v py); [ -n \"$PY\" ] && \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/scripts/track-search.py\" || exit 0'"
          }
        ]
      }
    ]
  }
}
